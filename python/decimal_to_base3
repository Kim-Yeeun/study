
# 10진수를 n진수로 변환하는 방법은
# 10진수를 n으로 나눈 결과를 거꾸로 정렬시키면 된다.

d = int(input("10진수 입력 : "))
t = ''

while d > 0:
    t = str(d % 3) + t
    d //= 3

print("3진수 : " + t)


# int 함수를 사용하여 n 진수를 10진수 d로 변환하는 방법
# n은 문자열로 삽입해도 상관없다.

d = int(n, 10)


